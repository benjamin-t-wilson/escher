import {SafeAreaView} from 'react-native';

import CardHolder from './components/cardHolder';

const App = () => {
  return (
    <SafeAreaView>
      <CardHolder />
    </SafeAreaView>
  );
};

export default App;
